{
  "name": "piq",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=26.0.0 <27"
  },
  "type": "module",
  "bin": {
    "pil": "./bin/piq.ts",
    "pic": "./bin/piq.ts",
    "pim": "./bin/piq.ts"
  },
  "scripts": {
    "build": "mkdir -p build && cc -O2 -Wall -Wextra -o build/tiocsti native/tiocsti.c",
    "prepare": "npm run build",
    "check": "biome check ."
  }
}
